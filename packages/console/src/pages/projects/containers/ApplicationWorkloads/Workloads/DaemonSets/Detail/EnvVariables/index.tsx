import React from 'react';
import EnvVariables from '../../../../../../../clusters/components/EnvVariables';

const Env = () => {
  return <EnvVariables detailPropsName={'DaemonSetDetailProps'} />;
};

export default Env;
