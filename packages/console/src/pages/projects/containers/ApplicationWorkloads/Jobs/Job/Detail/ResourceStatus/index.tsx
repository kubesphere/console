import React from 'react';
import ResourceStatus from '../../../../../../../clusters/components/ResourceStatus';

const Status = () => {
  return <ResourceStatus detailPropsName={'JobDetailProps'} />;
};

export default Status;
