import styled from 'styled-components';

const NameWrapper = styled.div`
  position: relative;

  img {
    position: absolute;
    top: 20px;
    right: 0px;
    background-color: #fff;
    border-radius: 50%;
    cursor: pointer;
  }
`;

export { NameWrapper };
