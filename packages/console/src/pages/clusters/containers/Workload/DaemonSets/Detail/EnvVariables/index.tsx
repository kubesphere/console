import React from 'react';
import EnvVariables from '../../../../../components/EnvVariables';

const Env = () => {
  return <EnvVariables detailPropsName={'DaemonSetDetailProps'} />;
};

export default Env;
