import styled from 'styled-components';

export const StepsWrapper = styled.div`
  .steps-content {
    position: relative;
    overflow-y: auto;
    max-height: calc(100vh - 222px);
    min-height: 500px;
    padding: 20px;
  }
`;
