import styled from 'styled-components';

import { Block, Title } from './shared.styles';

export const LogViewerWrapper = styled(Block)``;

export const LogViewerTitle = styled(Title)`
  margin-bottom: 8px;
`;
