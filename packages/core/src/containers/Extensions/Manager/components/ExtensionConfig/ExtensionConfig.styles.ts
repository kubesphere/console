import styled from 'styled-components';

import { LoadingWrapper as BaseLoadingWrapper } from '../shared.styles';

export { StepContentTitle } from '../shared.styles';

export const LoadingWrapper = styled(BaseLoadingWrapper)`
  height: 200px;
`;
