---
group: 'components'
title: AppInformation
description: '用于显示应用信息的卡片组件。'
---

# AppInformation 组件

用于显示应用信息的卡片组件。

## 示例

```jsx live=true
() => {
  return <AppInformation />;
}
```

在这个示例中，我们有一个 `AppInformation` 组件，用于显示应用的信息。

你可以直接使用 `AppInformation` 组件来展示应用的基本信息，包括应用详情和版本信息。