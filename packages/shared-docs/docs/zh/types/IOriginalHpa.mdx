## IOriginalHpa
```ts
interface IOriginalHpa extends KubernetesObjectBase {
  metadata: Required<
    Pick<
      ObjectMeta,
      | 'annotations'
      | 'creationTimestamp'
      | 'generation'
      | 'labels'
      | 'managedFields'
      | 'name'
      | 'namespace'
      | 'ownerReferences'
      | 'resourceVersion'
      | 'uid'
    >
  >;
  spec: {
    maxReplicas: number;
    metrics: IMetrics[];
    scaleTargetRef: {
      apiVersion: string;
      kind: string;
      name: string;
    };
  };
  status: {
    conditions: any | null;
    currentMetrics: any | null;
    currentReplicas: number;
    desiredReplicas: number;
  };
} 
```

