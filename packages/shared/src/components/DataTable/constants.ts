export const DEFAULT_WATCH_OPTIONS = {
  enabled: false,
  url: '',
  module: '',
};

export const DEFAULT_EMPTY_OPTIONS = {
  withoutTable: false,
  createButton: false,
};

export const DEFAULT_FILTERED_EMPTY_OPTIONS = {
  withoutTable: false,
  showRefetchButton: true,
  showClearAndRefetchButton: true,
};
